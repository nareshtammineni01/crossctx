# API Dependency Map

> Generated by CrossCtx v0.1.0 on 2026-04-10T06:18:19.327Z

## Services

### order-service

- **Base URL:** https://order-service.internal:8082
- **Spec version:** 3.0.3
- **Endpoints:** 3

### payment-service

- **Base URL:** https://payment-service.internal:8081
- **Spec version:** 3.0.3
- **Endpoints:** 3

### user-service

- **Base URL:** https://user-service.internal:8080
- **Spec version:** 3.0.3
- **Endpoints:** 3

## Endpoints

| Service | Method | Path | Summary |
|---------|--------|------|---------|
| order-service | GET | `/orders` | List all orders |
| order-service | POST | `/orders` | Create a new order |
| order-service | GET | `/orders/{id}` | Get order by ID |
| payment-service | POST | `/payments` | Process a payment |
| payment-service | GET | `/payments/{id}` | Get payment status |
| payment-service | POST | `/payments/{id}/refund` | Refund a payment |
| user-service | GET | `/users` | List all users |
| user-service | POST | `/users` | Create a new user |
| user-service | GET | `/users/{id}` | Get user by ID |

## Dependencies

```
order-service → user-service  (description)
order-service → payment-service  (description)
```

### Dependency Details

- **order-service** → **user-service**
  - Detected via: description
  - Evidence: Referenced URL: https://user-service.internal:8080
- **order-service** → **payment-service**
  - Detected via: description
  - Evidence: Referenced URL: https://payment-service.internal:8081

## Schemas (Simplified)

**GET /orders** (order-service)
- Response: [{ id: string, userId: string, total: number, status: string }]

**POST /orders** (order-service)
- Request: { userId: string, items: array, paymentMethod: string }
- Response: { id: string, status: string }

**GET /orders/{id}** (order-service)
- Response: { id: string, userId: string, items: array, total: number, status: string }

**POST /payments** (payment-service)
- Request: { orderId: string, amount: number, currency: string, method: string }
- Response: { id: string, status: string, transactionId: string }

**GET /payments/{id}** (payment-service)
- Response: { id: string, status: string, amount: number }

**POST /payments/{id}/refund** (payment-service)
- Response: { refundId: string, status: string }

**GET /users** (user-service)
- Response: [{ id: string, name: string, email: string }]

**POST /users** (user-service)
- Request: { name: string, email: string, password: string }
- Response: { id: string, name: string }

**GET /users/{id}** (user-service)
- Response: { id: string, name: string, email: string, createdAt: string }
